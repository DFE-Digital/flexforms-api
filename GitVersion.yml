mode: ContinuousDelivery
tag-prefix: 'v'
next-version: 2.1.4
major-version-bump-message: '(?i)\+semver:\s?(breaking|major)'
minor-version-bump-message: '(?i)\+semver:\s?(feature|minor)'
patch-version-bump-message: '(?i)\+semver:\s?(fix|patch)'

branches:
  main:
    increment: Patch
    label: ''

  pull-request:
    increment: Patch
    source-branches: ['main']
    label: prerelease
